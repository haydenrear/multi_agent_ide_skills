# Validation Workflows Reference

| Workflow | Description |
|----------|-------------|
| `validation_workflow.md` | **Canonical ticket collector validation workflow.** 9-step process: surface review → invariant mapping → exploration → test analysis → yield proposals → apply changes → run tests → post-test analysis → yield final analysis. Each yield point uses `call_controller`. |
